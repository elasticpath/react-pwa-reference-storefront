# ProductListMain

#### Description

Displays the items that belong to a specified product list or category. A shopper can select an item to see more details in the `ProductDisplayItemMain` component.

This component uses the `ProductListItemMain` component to display the item details.

#### Usage

```js
import { ProductListMain } from '@elasticpath/store-components';
```

#### Example

```js
<ProductListMain productData={productList} productListLinks={productLinks} />
```

#### Properties

<!-- PROPS -->
