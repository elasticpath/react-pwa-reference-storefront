pipeline {
    agent any
    stages {
        stage('Publish') {
            steps {
                sshagent (credentials: ['ep-github-sshkey-buildadmin']) {
                    sh 'docker build --build-arg NPM_TOKEN=$NPM_TOKEN -f ./docker/components/Dockerfile .'
                }
            }
        }
    }
}
